sample:
  name: broadcaster
tests:
  samples.bluetooth.broadcaster.atm:
    sysbuild: true
    harness: console
    harness_config:
      type: one_line
      regex:
        - Starting Broadcaster
    tags:
      - bluetooth
      - no_mcuboot
