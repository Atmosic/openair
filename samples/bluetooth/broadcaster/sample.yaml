sample:
  name: broadcaster
common:
  tags:
    - bluetooth
    - role_peripheral
    - role_broadcaster
tests:
  samples.bluetooth.broadcaster.atm:
    tags:
      - openair_release
    sysbuild: true
    harness: console
    harness_config:
      type: one_line
      regex:
        - Starting Broadcaster
