tests:
  samples.bluetooth.peripheral_identity.atm:
    sysbuild: true
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Advertising successfully started"
    tags:
      - bluetooth
      - no_mcuboot
