sample:
  name: Blink LED (PWM based)
tests:
  samples.basic.blinky_pwm.atm:
    sysbuild: true
    harness: console
    harness_config:
      type: one_line
      regex:
        - PWM-based blinky
    tags:
      - no_mcuboot
