sample:
  name: Fade LED
tests:
  samples.basic.fade_led.atm:
    sysbuild: true
    harness: console
    harness_config:
      type: multi_line
      ordered: true
      regex:
        - "Using pulse width \\d+%"
    tags:
      - no_mcuboot
