sample:
  description: minimal sample, the smallest possible Zephyr application
  name: minimal
tests:
  samples.basic.minimal.atm:
    sysbuild: true
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Hello World from minimal!"
    extra_configs:
      - CONFIG_SAMPLE_DO_OUTPUT=y
