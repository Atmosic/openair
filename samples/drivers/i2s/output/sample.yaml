common:
  tags:
    - atm_test_with_spe
    - introduction
sample:
  description: i2s TX output sample
  name: i2s output
tests:
  samples.drivers.i2s.output.atm:
    sysbuild: true
    harness: console
    harness_config:
      type: one_line
      regex:
        - "All I2S blocks written"
