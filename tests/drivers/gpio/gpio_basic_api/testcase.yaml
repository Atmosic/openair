tests:
  tests.drivers.gpio.gpio_basic_api.atm:
    sysbuild: true
    harness: console
    harness_config:
      type: one_line
      regex:
        - Running TESTSUITE after_flash_gpio_config_trigger
    tags:
      - no_mcuboot
