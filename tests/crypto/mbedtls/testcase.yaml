common:
  min_flash: 65
  min_ram: 36
  tags:
    - crypto
    - mbedtls
    - userspace
  timeout: 400
tests:
  tests.crypto.mbedtls.atm:
    sysbuild: true
    tags:
      - no_mcuboot
    extra_configs:
      - CONFIG_TEST_USERSPACE=n
